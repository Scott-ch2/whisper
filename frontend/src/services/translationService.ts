// translationService — speech-to-text and translation API
const API_BASE = '/api';

export async function translateAudio(audio: File, srcLang: string, tgtLang: string) {
  // TODO: POST /api/translate
  return { segments: [] };
}
